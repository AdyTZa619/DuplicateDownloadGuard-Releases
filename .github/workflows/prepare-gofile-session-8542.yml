name: Prepare GoFile durable session 8.5.42

on:
  push:
    branches: [fix/gofile-session-8542]

permissions:
  contents: write

jobs:
  prepare:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: fix/gofile-session-8542
          fetch-depth: 0
      - uses: actions/setup-go@v5
        with:
          go-version: '1.23.x'
      - name: Patch durable GoFile session
        shell: bash
        run: |
          python3 - <<'PY'
          from pathlib import Path
          import re

          session = r'''package main

import (
    "crypto/sha256"
    "encoding/hex"
    "encoding/json"
    "errors"
    "fmt"
    "net/http"
    "os"
    "path/filepath"
    "strconv"
    "strings"
    "sync"
    "time"
)

const gofileGuestStateVersionV8542 = 1
const gofileCurrentWebsiteSaltV8542 = "12af056dacea0b"

var gofileGuestStateMuV8542 sync.Mutex

type gofileGuestStateV8542 struct {
    Version       int    `json:"version"`
    Token         string `json:"token,omitempty"`
    CreatedAt     int64  `json:"createdAt,omitempty"`
    CooldownUntil int64  `json:"cooldownUntil,omitempty"`
    WorkingSalt   string `json:"workingSalt,omitempty"`
}

type gofileAPIErrorV8542 struct {
    HTTPStatus int
    APIStatus  string
}

func (e *gofileAPIErrorV8542) Error() string {
    status := strings.TrimSpace(e.APIStatus)
    if status != "" {
        return fmt.Sprintf("GoFile API HTTP %d (%s)", e.HTTPStatus, status)
    }
    return fmt.Sprintf("GoFile API HTTP %d", e.HTTPStatus)
}

func gofileGuestStatePathV8542() string {
    if override := strings.TrimSpace(os.Getenv("DDG_GOFILE_STATE_PATH")); override != "" {
        return override
    }
    if dataDir, err := portableDataDir(); err == nil {
        return filepath.Join(dataDir, "cache", "gofile_guest_state.json")
    }
    return filepath.Join(executableDir(), "data", "cache", "gofile_guest_state.json")
}

func loadGofileGuestStateUnlockedV8542() gofileGuestStateV8542 {
    var state gofileGuestStateV8542
    data, err := os.ReadFile(gofileGuestStatePathV8542())
    if err != nil {
        return state
    }
    if json.Unmarshal(data, &state) != nil || state.Version != gofileGuestStateVersionV8542 {
        return gofileGuestStateV8542{}
    }
    return state
}

func saveGofileGuestStateUnlockedV8542(state gofileGuestStateV8542) error {
    state.Version = gofileGuestStateVersionV8542
    path := gofileGuestStatePathV8542()
    if err := os.MkdirAll(filepath.Dir(path), 0755); err != nil {
        return err
    }
    data, err := json.MarshalIndent(state, "", "  ")
    if err != nil {
        return err
    }
    data = append(data, '\n')
    tmp := path + ".tmp"
    if err := os.WriteFile(tmp, data, 0600); err != nil {
        return err
    }
    _ = os.Remove(path)
    if err := os.Rename(tmp, path); err != nil {
        _ = os.Remove(tmp)
        return err
    }
    return nil
}

func loadCachedGofileGuestTokenV8542() (string, bool) {
    gofileGuestStateMuV8542.Lock()
    defer gofileGuestStateMuV8542.Unlock()
    state := loadGofileGuestStateUnlockedV8542()
    token := strings.TrimSpace(state.Token)
    return token, token != ""
}

func persistGofileGuestTokenV8542(token string) error {
    token = strings.TrimSpace(token)
    if token == "" {
        return errors.New("token GoFile gol")
    }
    gofileGuestStateMuV8542.Lock()
    defer gofileGuestStateMuV8542.Unlock()
    state := loadGofileGuestStateUnlockedV8542()
    state.Token = token
    state.CreatedAt = time.Now().Unix()
    state.CooldownUntil = 0
    return saveGofileGuestStateUnlockedV8542(state)
}

func clearCachedGofileGuestTokenV8542() {
    gofileGuestStateMuV8542.Lock()
    defer gofileGuestStateMuV8542.Unlock()
    state := loadGofileGuestStateUnlockedV8542()
    state.Token = ""
    state.CreatedAt = 0
    _ = saveGofileGuestStateUnlockedV8542(state)
}

func gofileConfiguredTokenV8542() string {
    for _, key := range []string{"GOFILE_TOKEN", "GOFILE_API_TOKEN", "GF_TOKEN"} {
        if token := strings.TrimSpace(os.Getenv(key)); token != "" {
            return token
        }
    }
    return ""
}

func setGofileGuestCooldownV8542(delay time.Duration) {
    if delay <= 0 {
        return
    }
    gofileGuestStateMuV8542.Lock()
    defer gofileGuestStateMuV8542.Unlock()
    state := loadGofileGuestStateUnlockedV8542()
    until := time.Now().Add(delay).Unix()
    if until > state.CooldownUntil {
        state.CooldownUntil = until
        _ = saveGofileGuestStateUnlockedV8542(state)
    }
}

func clearGofileGuestCooldownV8542() {
    gofileGuestStateMuV8542.Lock()
    defer gofileGuestStateMuV8542.Unlock()
    state := loadGofileGuestStateUnlockedV8542()
    if state.CooldownUntil != 0 {
        state.CooldownUntil = 0
        _ = saveGofileGuestStateUnlockedV8542(state)
    }
}

func gofileGuestCooldownRemainingV8542() time.Duration {
    gofileGuestStateMuV8542.Lock()
    defer gofileGuestStateMuV8542.Unlock()
    state := loadGofileGuestStateUnlockedV8542()
    if state.CooldownUntil <= 0 {
        return 0
    }
    remaining := time.Until(time.Unix(state.CooldownUntil, 0))
    if remaining <= 0 {
        state.CooldownUntil = 0
        _ = saveGofileGuestStateUnlockedV8542(state)
        return 0
    }
    return remaining
}

func rememberWorkingGofileSaltV8542(salt string) {
    salt = strings.TrimSpace(salt)
    if salt == "" {
        return
    }
    gofileGuestStateMuV8542.Lock()
    defer gofileGuestStateMuV8542.Unlock()
    state := loadGofileGuestStateUnlockedV8542()
    if state.WorkingSalt == salt {
        return
    }
    state.WorkingSalt = salt
    _ = saveGofileGuestStateUnlockedV8542(state)
}

func cachedWorkingGofileSaltV8542() string {
    gofileGuestStateMuV8542.Lock()
    defer gofileGuestStateMuV8542.Unlock()
    return strings.TrimSpace(loadGofileGuestStateUnlockedV8542().WorkingSalt)
}

func gofileWebsiteSaltCandidatesV8542() []string {
    out := make([]string, 0, 4)
    seen := map[string]bool{}
    add := func(s string) {
        s = strings.TrimSpace(s)
        if s == "" || seen[s] {
            return
        }
        seen[s] = true
        out = append(out, s)
    }
    add(os.Getenv("GOFILE_WT_SALT"))
    add(cachedWorkingGofileSaltV8542())
    add(gofileCurrentWebsiteSaltV8542)
    // Known historical values are fallback candidates only. Reusing the same
    // account token with another website-token candidate is cheap and avoids
    // creating throw-away guest accounts when GoFile rotates its web token.
    add("9844d94d963d30")
    add("5d4f7g8sd45fsd")
    return out
}

func gofileWebsiteTokenForSaltV8542(accountToken, salt string, now time.Time) string {
    bucket := now.Unix() / 14400
    raw := gofileUserAgentV86 + "::en-US::" + accountToken + "::" + strconv.FormatInt(bucket, 10) + "::" + salt
    sum := sha256.Sum256([]byte(raw))
    return hex.EncodeToString(sum[:])
}

func gofileAPIErrorInfoV8542(err error) (int, string, bool) {
    var apiErr *gofileAPIErrorV8542
    if !errors.As(err, &apiErr) {
        return 0, "", false
    }
    return apiErr.HTTPStatus, strings.ToLower(strings.TrimSpace(apiErr.APIStatus)), true
}

func gofileAccountTokenRejectedV8542(err error) bool {
    _, status, ok := gofileAPIErrorInfoV8542(err)
    if !ok {
        return false
    }
    switch status {
    case "error-wrongtoken", "error-notauthenticated", "error-badtoken", "error-invalidtoken":
        return true
    default:
        return false
    }
}

func gofileWebsiteTokenCandidateErrorV8542(err error) bool {
    httpStatus, status, ok := gofileAPIErrorInfoV8542(err)
    if !ok {
        return false
    }
    if gofileAccountTokenRejectedV8542(err) {
        return false
    }
    if httpStatus == http.StatusUnauthorized || httpStatus == http.StatusForbidden {
        return true
    }
    switch status {
    case "error-notpremium", "error-websitetoken", "error-wrongwebsitetoken":
        return true
    default:
        return false
    }
}

func gofileRateLimitedV8542(err error) bool {
    httpStatus, status, ok := gofileAPIErrorInfoV8542(err)
    if ok && (httpStatus == http.StatusTooManyRequests || status == "error-ratelimit") {
        return true
    }
    text := strings.ToLower(err.Error())
    return strings.Contains(text, "guest account rate-limit") || strings.Contains(text, "guest account în cooldown") || strings.Contains(text, "guest account in cooldown")
}

func gofileGuestCooldownDelayV8542(resp *http.Response) time.Duration {
    delay := 5 * time.Minute
    if resp != nil {
        raw := strings.TrimSpace(resp.Header.Get("Retry-After"))
        if seconds, err := strconv.Atoi(raw); err == nil && seconds > 0 {
            delay = time.Duration(seconds) * time.Second
        } else if when, err := http.ParseTime(raw); err == nil {
            if d := time.Until(when); d > 0 {
                delay = d
            }
        }
    }
    if delay < 30*time.Second {
        delay = 30 * time.Second
    }
    if delay > 30*time.Minute {
        delay = 30 * time.Minute
    }
    return delay
}

func invalidatePersistentGofileGuestTokenV8542() {
    invalidateGoFileGuestTokenV86()
    clearCachedGofileGuestTokenV8542()
}
'''
          Path('provider_gofile_session_v8542.go').write_text(session, encoding='utf-8')

          p = Path('provider_gofile_native.go')
          s = p.read_text(encoding='utf-8')
          s = s.replace('const gofileWebsiteSaltDefaultV86 = "9844d94d963d30"', 'const gofileWebsiteSaltDefaultV86 = gofileCurrentWebsiteSaltV8542')
          s = s.replace('const gofileUserAgentV86 = "Mozilla/5.0 (Windows NT 10.0; Win64; x64) DuplicateDownloadGuard"', 'const gofileUserAgentV86 = "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/126.0.0.0 Safari/537.36"')

          replacement = r'''func fetchGofileContentOneSaltV8542(ctx context.Context, client *http.Client, token, contentID, salt string) (gofileContentV86, error) {
    endpoint := "https://api.gofile.io/contents/" + url.PathEscape(contentID)
    q := url.Values{}
    q.Set("contentFilter", "")
    q.Set("page", "1")
    q.Set("pageSize", "1000")
    q.Set("sortField", "createTime")
    q.Set("sortDirection", "-1")
    endpoint += "?" + q.Encode()

    for attempt := 0; attempt <= gofileAPIMax429RetriesV86; attempt++ {
        if err := waitGofileAPISlotV86(ctx); err != nil {
            return gofileContentV86{}, err
        }
        req, err := http.NewRequestWithContext(ctx, http.MethodGet, endpoint, nil)
        if err != nil {
            return gofileContentV86{}, err
        }
        req.Header.Set("User-Agent", gofileUserAgentV86)
        req.Header.Set("Accept", "application/json")
        req.Header.Set("Authorization", "Bearer "+token)
        req.Header.Set("X-Website-Token", gofileWebsiteTokenForSaltV8542(token, salt, time.Now()))
        req.Header.Set("X-BL", "en-US")
        req.Header.Set("Origin", "https://gofile.io")
        req.Header.Set("Referer", "https://gofile.io/")

        resp, err := client.Do(req)
        if err != nil {
            return gofileContentV86{}, fmt.Errorf("GoFile API indisponibil: %w", err)
        }
        delay := time.Duration(0)
        if resp.StatusCode == http.StatusTooManyRequests {
            delay = gofileRetryAfterV86(resp)
        }
        var env gofileContentEnvelopeV86
        decodeErr := json.NewDecoder(resp.Body).Decode(&env)
        _ = resp.Body.Close()

        apiStatus := strings.TrimSpace(env.Status)
        if resp.StatusCode == http.StatusTooManyRequests || strings.EqualFold(apiStatus, "error-rateLimit") {
            if attempt >= gofileAPIMax429RetriesV86 {
                return gofileContentV86{}, &gofileAPIErrorV8542{HTTPStatus: http.StatusTooManyRequests, APIStatus: apiStatus}
            }
            if delay <= 0 {
                delay = 5 * time.Second
            }
            if err := gofileSleepV86(ctx, delay); err != nil {
                return gofileContentV86{}, err
            }
            continue
        }

        if resp.StatusCode >= 200 && resp.StatusCode < 300 && decodeErr == nil && strings.EqualFold(apiStatus, "ok") {
            return env.Data, nil
        }
        if resp.StatusCode >= 200 && resp.StatusCode < 300 && decodeErr != nil {
            return gofileContentV86{}, fmt.Errorf("răspuns GoFile invalid: %w", decodeErr)
        }
        if resp.StatusCode >= 200 && resp.StatusCode < 300 {
            return gofileContentV86{}, &gofileAPIErrorV8542{HTTPStatus: resp.StatusCode, APIStatus: apiStatus}
        }
        return gofileContentV86{}, &gofileAPIErrorV8542{HTTPStatus: resp.StatusCode, APIStatus: apiStatus}
    }
    return gofileContentV86{}, errors.New("GoFile API: limită de reîncercări depășită")
}

func fetchGofileContentV86(ctx context.Context, client *http.Client, token, contentID string) (gofileContentV86, error) {
    candidates := gofileWebsiteSaltCandidatesV8542()
    var lastErr error
    for i, salt := range candidates {
        data, err := fetchGofileContentOneSaltV8542(ctx, client, token, contentID, salt)
        if err == nil {
            rememberWorkingGofileSaltV8542(salt)
            return data, nil
        }
        lastErr = err
        if gofileAccountTokenRejectedV8542(err) || !gofileWebsiteTokenCandidateErrorV8542(err) || i == len(candidates)-1 {
            break
        }
    }
    if lastErr == nil {
        lastErr = errors.New("GoFile nu are niciun website-token candidat")
    }
    return gofileContentV86{}, lastErr
}

func gofileAppendFileV86'''
          s2, n = re.subn(r'func fetchGofileContentV86\(.*?\n\}\n\nfunc gofileAppendFileV86', replacement, s, count=1, flags=re.S)
          if n != 1:
              raise SystemExit('failed replacing fetchGofileContentV86')
          s = s2

          old = '''\troot, err := fetchGofileContentV86(ctx, client, token, code)\n\tif err != nil && strings.Contains(err.Error(), "HTTP 401") {\n\t\ta.logf("GoFile: 401 de la metadata; refac tokenul guest și reîncerc o singură dată")\n\t\tinvalidateGoFileGuestTokenV86()\n\t\tif freshToken, tokenErr := gofileGuestTokenV86(ctx, nil); tokenErr == nil {\n\t\t\ttoken = freshToken\n\t\t\troot, err = fetchGofileContentV86(ctx, client, token, code)\n\t\t} else {\n\t\t\terr = tokenErr\n\t\t}\n\t}\n'''
          new = '''\troot, err := fetchGofileContentV86(ctx, client, token, code)\n\tif err != nil && gofileAccountTokenRejectedV8542(err) && gofileConfiguredTokenV8542() == "" {\n\t\ta.logf("GoFile: API a confirmat token guest invalid; șterg cache-ul și îl refac o singură dată")\n\t\tinvalidatePersistentGofileGuestTokenV8542()\n\t\tif freshToken, tokenErr := gofileGuestTokenV86(ctx, nil); tokenErr == nil {\n\t\t\ttoken = freshToken\n\t\t\troot, err = fetchGofileContentV86(ctx, client, token, code)\n\t\t} else {\n\t\t\terr = tokenErr\n\t\t}\n\t}\n'''
          if old not in s:
              raise SystemExit('failed locating native 401 refresh block')
          s = s.replace(old, new, 1)
          p.write_text(s, encoding='utf-8')

          p = Path('provider_http_transport.go')
          s = p.read_text(encoding='utf-8')
          replacement = r'''func gofileGuestTokenV86(ctx context.Context, base http.RoundTripper) (string, error) {
    gofileTokenMuV86.Lock()
    defer gofileTokenMuV86.Unlock()

    // Reuse one account token for the whole process. Unlike the old 3-hour
    // timer, validity is decided by GoFile itself when metadata is requested.
    if token := strings.TrimSpace(gofileTokenV86); token != "" {
        return token, nil
    }
    if configured := gofileConfiguredTokenV8542(); configured != "" {
        gofileTokenV86 = configured
        gofileTokenAtV86 = time.Now()
        return configured, nil
    }
    if cached, ok := loadCachedGofileGuestTokenV8542(); ok {
        gofileTokenV86 = cached
        gofileTokenAtV86 = time.Now()
        return cached, nil
    }
    if remaining := gofileGuestCooldownRemainingV8542(); remaining > 0 {
        return "", fmt.Errorf("GoFile guest account în cooldown încă %s după rate-limit", remaining.Round(time.Second))
    }

    if base == nil {
        if wrapped, ok := http.DefaultTransport.(*providerAwareTransportV86); ok && wrapped.base != nil {
            base = wrapped.base
        } else {
            base = http.DefaultTransport
        }
    }

    const maxAttempts = 3
    const attemptTimeout = 15 * time.Second
    var lastErr error
    for attempt := 0; attempt < maxAttempts; attempt++ {
        attemptCtx, cancel := context.WithTimeout(ctx, attemptTimeout)
        req, err := http.NewRequestWithContext(attemptCtx, http.MethodPost, "https://api.gofile.io/accounts", bytes.NewBufferString("{}"))
        if err != nil {
            cancel()
            return "", err
        }
        req.Header.Set("Origin", "https://gofile.io")
        req.Header.Set("Referer", "https://gofile.io/")
        req.Header.Set("User-Agent", gofileUserAgentV86)
        req.Header.Set("Accept", "application/json")
        req.Header.Set("Content-Type", "application/json")
        req.Header.Set("X-BL", "en-US")
        req.Header.Set("X-Website-Token", gofileWebsiteTokenV86("", time.Now()))

        client := &http.Client{Transport: base}
        resp, err := client.Do(req)
        if err != nil {
            cancel()
            if ctx.Err() != nil {
                return "", ctx.Err()
            }
            lastErr = fmt.Errorf("încercarea %d/%d: %w", attempt+1, maxAttempts, err)
            if attempt+1 < maxAttempts {
                if sleepErr := gofileSleepV86(ctx, time.Duration(attempt+1)*time.Second); sleepErr != nil {
                    return "", sleepErr
                }
                continue
            }
            break
        }

        status := resp.StatusCode
        var env struct {
            Status string `json:"status"`
            Data struct {
                Token string `json:"token"`
            } `json:"data"`
        }
        decodeErr := json.NewDecoder(resp.Body).Decode(&env)
        rateLimited := status == http.StatusTooManyRequests || strings.EqualFold(strings.TrimSpace(env.Status), "error-rateLimit")
        cooldown := time.Duration(0)
        if rateLimited {
            cooldown = gofileGuestCooldownDelayV8542(resp)
        }
        _ = resp.Body.Close()
        cancel()

        if rateLimited {
            setGofileGuestCooldownV8542(cooldown)
            return "", fmt.Errorf("GoFile guest account rate-limit; nu mai creez alte conturi timp de %s", cooldown.Round(time.Second))
        }
        if status >= 200 && status < 300 && decodeErr == nil && strings.EqualFold(strings.TrimSpace(env.Status), "ok") && strings.TrimSpace(env.Data.Token) != "" {
            token := strings.TrimSpace(env.Data.Token)
            gofileTokenV86 = token
            gofileTokenAtV86 = time.Now()
            clearGofileGuestCooldownV8542()
            _ = persistGofileGuestTokenV8542(token)
            return token, nil
        }
        if status == http.StatusRequestTimeout || status == http.StatusTooEarly || status >= 500 {
            lastErr = fmt.Errorf("GoFile guest account HTTP %d", status)
            if attempt+1 < maxAttempts {
                if sleepErr := gofileSleepV86(ctx, time.Duration(attempt+1)*time.Second); sleepErr != nil {
                    return "", sleepErr
                }
                continue
            }
            break
        }
        if decodeErr != nil {
            return "", fmt.Errorf("răspuns GoFile guest invalid: %w", decodeErr)
        }
        return "", fmt.Errorf("GoFile guest account HTTP %d (%s)", status, strings.TrimSpace(env.Status))
    }
    if lastErr == nil {
        lastErr = errors.New("eroare necunoscută")
    }
    return "", fmt.Errorf("GoFile guest account indisponibil după %d încercări: %w", maxAttempts, lastErr)
}

func detectGalleryDLForProviderV86'''
          s2, n = re.subn(r'func gofileGuestTokenV86\(.*?\n\}\n\nfunc detectGalleryDLForProviderV86', replacement, s, count=1, flags=re.S)
          if n != 1:
              raise SystemExit('failed replacing gofileGuestTokenV86')
          p.write_text(s2, encoding='utf-8')

          p = Path('provider_gallery_scan.go')
          s = p.read_text(encoding='utf-8')
          old = '''\t\tnativeErr = err\n\t\tif err != nil {\n\t\t\ta.logf("GoFile: adapterul nativ a eșuat; încerc fallback gallery-dl: %v", err)\n\t\t}\n'''
          new = '''\t\tnativeErr = err\n\t\tif err != nil {\n\t\t\tif gofileRateLimitedV8542(err) {\n\t\t\t\ta.logf("GoFile: rate-limit detectat; opresc scanarea fără fallback ca să nu creez alte conturi guest: %v", err)\n\t\t\t\treturn nil, err\n\t\t\t}\n\t\t\ta.logf("GoFile: adapterul nativ a eșuat; încerc fallback gallery-dl: %v", err)\n\t\t}\n'''
          if old not in s:
              raise SystemExit('failed locating gallery fallback block')
          p.write_text(s.replace(old, new, 1), encoding='utf-8')

          tests = r'''package main

import (
    "context"
    "io"
    "net/http"
    "os"
    "path/filepath"
    "strings"
    "testing"
    "time"
)

type gofileRoundTripV8542 func(*http.Request) (*http.Response, error)
func (f gofileRoundTripV8542) RoundTrip(r *http.Request) (*http.Response, error) { return f(r) }

func setupGofileStateTestV8542(t *testing.T) string {
    t.Helper()
    path := filepath.Join(t.TempDir(), "gofile-state.json")
    t.Setenv("DDG_GOFILE_STATE_PATH", path)
    t.Setenv("GOFILE_TOKEN", "")
    t.Setenv("GOFILE_API_TOKEN", "")
    t.Setenv("GF_TOKEN", "")
    t.Setenv("GOFILE_WT_SALT", "")
    invalidateGoFileGuestTokenV86()
    return path
}

func TestGofileGuestTokenPersistsAcrossMemoryResetV8542(t *testing.T) {
    setupGofileStateTestV8542(t)
    if err := persistGofileGuestTokenV8542("persisted-guest"); err != nil { t.Fatal(err) }
    invalidateGoFileGuestTokenV86()
    calls := 0
    tr := gofileRoundTripV8542(func(r *http.Request) (*http.Response, error) {
        calls++
        return nil, context.DeadlineExceeded
    })
    token, err := gofileGuestTokenV86(context.Background(), tr)
    if err != nil { t.Fatal(err) }
    if token != "persisted-guest" || calls != 0 {
        t.Fatalf("token=%q calls=%d", token, calls)
    }
}

func TestGofileConfiguredTokenIsNotPersistedV8542(t *testing.T) {
    path := setupGofileStateTestV8542(t)
    t.Setenv("GOFILE_TOKEN", "configured-account")
    token, err := gofileGuestTokenV86(context.Background(), gofileRoundTripV8542(func(r *http.Request) (*http.Response, error) {
        t.Fatal("network must not be used for configured token")
        return nil, nil
    }))
    if err != nil || token != "configured-account" { t.Fatalf("token=%q err=%v", token, err) }
    if data, err := os.ReadFile(path); err == nil && strings.Contains(string(data), "configured-account") {
        t.Fatal("configured token was persisted")
    }
}

func TestGofileGuest429CreatesCooldownAndDoesNotHammerV8542(t *testing.T) {
    setupGofileStateTestV8542(t)
    calls := 0
    tr := gofileRoundTripV8542(func(r *http.Request) (*http.Response, error) {
        calls++
        return &http.Response{
            StatusCode: http.StatusTooManyRequests,
            Header: http.Header{"Retry-After": []string{"60"}},
            Body: io.NopCloser(strings.NewReader(`{"status":"error-rateLimit"}`)),
            Request: r,
        }, nil
    })
    _, err := gofileGuestTokenV86(context.Background(), tr)
    if err == nil || calls != 1 { t.Fatalf("err=%v calls=%d", err, calls) }
    invalidateGoFileGuestTokenV86()
    _, err = gofileGuestTokenV86(context.Background(), tr)
    if err == nil || calls != 1 { t.Fatalf("cooldown did not stop second POST: err=%v calls=%d", err, calls) }
}

func TestGofileCurrentSaltAndCandidateFallbackV8542(t *testing.T) {
    setupGofileStateTestV8542(t)
    if gofileWebsiteSaltDefaultV86 != "12af056dacea0b" {
        t.Fatalf("current salt=%q", gofileWebsiteSaltDefaultV86)
    }
    calls := 0
    client := &http.Client{Transport: gofileRoundTripV8542(func(r *http.Request) (*http.Response, error) {
        calls++
        if calls == 1 {
            return &http.Response{StatusCode: http.StatusUnauthorized, Header: make(http.Header), Body: io.NopCloser(strings.NewReader(`{"status":"error-notPremium"}`)), Request: r}, nil
        }
        return &http.Response{StatusCode: http.StatusOK, Header: make(http.Header), Body: io.NopCloser(strings.NewReader(`{"status":"ok","data":{"id":"folder","type":"folder","name":"root","children":{}}}`)), Request: r}, nil
    })}
    ctx, cancel := context.WithTimeout(context.Background(), 5*time.Second)
    defer cancel()
    _, err := fetchGofileContentV86(ctx, client, "same-account-token", "folder")
    if err != nil { t.Fatal(err) }
    if calls != 2 { t.Fatalf("expected website-token fallback with same account, calls=%d", calls) }
}

func TestGofileExplicitWrongTokenIsDistinguishedV8542(t *testing.T) {
    err := &gofileAPIErrorV8542{HTTPStatus: http.StatusUnauthorized, APIStatus: "error-wrongToken"}
    if !gofileAccountTokenRejectedV8542(err) { t.Fatal("wrongToken not classified as account token rejection") }
    if gofileWebsiteTokenCandidateErrorV8542(err) { t.Fatal("wrongToken incorrectly classified as website-token rotation") }
    generic := &gofileAPIErrorV8542{HTTPStatus: http.StatusUnauthorized, APIStatus: "error-notPremium"}
    if gofileAccountTokenRejectedV8542(generic) { t.Fatal("notPremium must not immediately destroy guest cache") }
    if !gofileWebsiteTokenCandidateErrorV8542(generic) { t.Fatal("notPremium should try website-token candidates first") }
}
'''
          Path('provider_gofile_session_v8542_test.go').write_text(tests, encoding='utf-8')
          PY
          gofmt -w provider_gofile_session_v8542.go provider_gofile_session_v8542_test.go provider_gofile_native.go provider_http_transport.go provider_gallery_scan.go
          go test ./...
          go vet ./...
      - name: Commit patch and remove helper
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add provider_gofile_session_v8542.go provider_gofile_session_v8542_test.go provider_gofile_native.go provider_http_transport.go provider_gallery_scan.go
          git rm .github/workflows/prepare-gofile-session-8542.yml
          git commit -m "Fix GoFile guest session lifecycle and rate-limit churn"
          git push origin HEAD:fix/gofile-session-8542
